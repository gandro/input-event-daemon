#define test_bit(bit, array) ((array)[(bit)/8] & (1 << ((bit)%8)))

static char *listen[32]; // everything is NULL

#define MAX_CHAIN 3

struct key_event {
    unsigned short int code;
    int value;
    key_event *chain[MAX_CHAIN];
    char exec[1024];
};
